# Contribute

## How to add sections

* Add the .mdx file(s) or folders in `/docs`.
* Add the new reference on the sidebar (`/sidebar.ts`) file.
* Add the new button on the main page (`/src/pages/index.tsx`)

## How to run the project locally

* `/cd` to the folder.
